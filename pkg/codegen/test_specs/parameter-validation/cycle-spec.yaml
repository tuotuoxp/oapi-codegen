openapi: 3.0.3
info:
  title: parameter validation cycle
  version: "1.0"
paths:
  /cycle:
    get:
      operationId: getCycle
      parameters:
        - name: q
          in: query
          required: true
          schema:
            $ref: ./schemas/cycle-a.yaml
      responses:
        "200":
          description: ok
